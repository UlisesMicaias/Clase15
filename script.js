// const user = {id: 1, name: "micaias", surname: "ulises", age: 18};
// const nombreDelUsuario = user.name;

const {id, age, surname} = usuario; //--->> desestructurar un objeto, lo que hace es tomar cada propiedad, osea el key de un objeto y trayendo su value a una variable de modo que nos ahorramos codigo
//--->>> esto es igual que const id = user.id; const age = user.age; const surname = user.surname;